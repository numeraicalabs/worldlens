# ── Web framework ──────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9

# ── Auth / security ────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=3.2.0,<4.0.0

# ── Async / storage ────────────────────────────────────────────
apscheduler>=3.10.4
aiosqlite>=0.20.0
aiofiles>=23.2.1

# ── HTTP client ────────────────────────────────────────────────
httpx>=0.27.0

# ── Config / data ──────────────────────────────────────────────
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-dotenv>=1.0.1

# ── Finance data ───────────────────────────────────────────────
yfinance>=0.2.40
feedparser>=6.0.11

# ── AI cloud providers ─────────────────────────────────────────
anthropic>=0.25.0
# google-generativeai optional (using raw httpx calls to Gemini)

# ══════════════════════════════════════════════════════════════
# ML INTELLIGENCE LAYER  (all optional — app runs without them)
# Install group: pip install -r requirements.txt --extra-index-url ...
# Or individually as shown below.
# ══════════════════════════════════════════════════════════════

# ── FinBERT / BERT sentiment ───────────────────────────────────
# Downloads ProsusAI/finbert (~500MB) on first use.
# CPU inference, no GPU required.
#torch>=2.1.0                        # CPU build is sufficient
#transformers>=4.40.0                # HuggingFace transformers
# sentence-transformers>=2.7.0       # optional: richer embeddings (adds 300MB)

# ── Named Entity Recognition (spaCy) ──────────────────────────
# After installing, run: python -m spacy download en_core_web_sm
#spacy>=3.7.0

# ── Knowledge Graph ────────────────────────────────────────────
# Pure-Python, lightweight.
#networkx>=3.3

# ── Data analysis (for advanced quant features) ────────────────
#
numpy>=1.26.0                      # optional: faster PCA/regression
pandas>=2.2.0                      # optional: data manipulation
